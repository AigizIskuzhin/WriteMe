@model IEnumerable<Website.ViewModels.Friends.FriendViewModel>
@{ Layout = null;}
@if (Model.Any())
{
    @foreach (var friend in Model)
    {
        await Html.RenderPartialAsync("FriendView", friend);
    }
}